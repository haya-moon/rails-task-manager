<h1>Task Details</h2>

<strong>Title:</strong>
<%= @task.title %>
<br>
<strong>Title:</strong>
<%= @task.details %>
<br>
<strong>Completed:</strong>
<%= @task.completed ? 'Y' : 'N' %>

<%= link_to 'Delete',
task_path(@task),
data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
